'use strict';

angular.module('exampleApp')
  .config(function ($stateProvider) {
    $stateProvider
      .state('change', {
        url: '/change?sptoken',
        templateUrl: "<%= asset_path('change-password/change-password.html') %>"
      });
  });
